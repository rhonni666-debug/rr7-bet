import { ArrowDownLeft, ArrowUpRight, ShieldCheck } from 'lucide-react';
import { useDemo } from '../lib/demo-store';

export function WalletPage() {
  const { balance, transactions } = useDemo();
  return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[.16em] text-amber-300">Carteira</p><h1 className="mt-1 text-3xl font-black">Créditos DEMO</h1></header><section className="overflow-hidden rounded-3xl border border-amber-300/20 bg-[radial-gradient(circle_at_85%_10%,rgba(251,191,36,.22),transparent_28%),linear-gradient(135deg,#123844,#0a2029)] p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-400">Saldo demonstrativo</p><p className="mt-2 text-4xl font-black text-amber-300">{balance.toLocaleString('pt-BR')}</p><p className="mt-1 text-sm text-slate-400">créditos sem valor monetário</p><div className="mt-5 flex items-center gap-2 text-xs font-semibold text-emerald-300"><ShieldCheck className="h-4 w-4" /> Sem PIX, depósito ou saque</div></section><section><h2 className="mb-3 text-lg font-black">Ledger local</h2><div className="overflow-hidden rounded-2xl border border-white/8 bg-white/[.035]">{[...transactions].reverse().map((item) => <div key={item.id} className="flex items-center justify-between border-b border-white/6 px-4 py-3 last:border-b-0"><div className="flex min-w-0 items-center gap-3"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl ${item.amount >= 0 ? 'bg-emerald-400/10 text-emerald-300' : 'bg-rose-400/10 text-rose-300'}`}>{item.amount >= 0 ? <ArrowDownLeft className="h-4 w-4" /> : <ArrowUpRight className="h-4 w-4" />}</span><div className="min-w-0"><p className="truncate text-sm font-bold">{item.description}</p><p className="text-[11px] text-slate-500">{new Date(item.createdAt).toLocaleString('pt-BR')}</p></div></div><p className={`ml-3 shrink-0 text-sm font-black ${item.amount >= 0 ? 'text-emerald-300' : 'text-rose-300'}`}>{item.amount > 0 ? '+' : ''}{item.amount.toLocaleString('pt-BR')}</p></div>)}</div></section></div>;
}
