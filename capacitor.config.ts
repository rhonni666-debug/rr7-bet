import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rr7.bet',
  appName: 'RR7.BET Demo',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
